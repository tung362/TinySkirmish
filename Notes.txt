XAVHJ
XAVHJ
XAVHJ

even if object is childed to something else the list order will still be the same

String Generator:
if(stringlist.count == 0)
{
	name = currentnumber;
	currentnumber += 1;
}
else
{
	name = stringlist[0];
	stringlist.removeat(0);
}


Way of syncing
have transform sync still track block's childs
Only allow blocks to child with other blocks

Sync Name
Have whatever is spawning assign the name's number
have blocks be incharge of renameing childs(take in numbers only)